project:
  title: "LLM-Augmented QSP Platform — KSMB 2026"
  output-dir: _output

# Counts are centralized in _variables.yml (refresh with scripts/library-stats.sh).
# Reference them in slides via the var shortcode, e.g. {{< var n_models >}}.
